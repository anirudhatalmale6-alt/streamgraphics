Put the SIGNED "StreamGraphics Pro.exe" launcher in this folder.

build.sh uses it verbatim and skips compiling a fresh (unsigned) one. See BUILD.md
for why the launcher needs a signature of its own and why signing it once is enough.
